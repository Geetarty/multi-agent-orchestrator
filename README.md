# multi-agent-orchestrator
TypeScript/Node HTTP API system orchestrating multi-agent C code compilation and patching. Includes Orchestrator (coordination), Compiler Agent (analysis), Fixer Agent (patch generation). Features infinite loop with stall detection, adaptive scoring, automatic escalation, and production-ready error handling.
